import { Row, Col } from "react-bootstrap";
import { useContext } from "react";

export const ContainerMobile = () => {
  return (
    <Row className="bg-success">
      <Col>
        <h1>Container Mobile</h1>
      </Col>
    </Row>
  );
};
